{
  "max_items": {
    "name": "显示条数",
    "type": "integer",
    "default": 15,
    "hint": "每次显示的热搜条数（1-30）"
  },
  "enable_auto_push": {
    "name": "自动推送",
    "type": "switch",
    "default": false,
    "hint": "是否每天早上自动推送热搜简报"
  },
  "push_time": {
    "name": "推送时间",
    "type": "string",
    "default": "08:00",
    "hint": "自动推送时间（格式 HH:MM）"
  },
  "push_targets": {
    "name": "推送目标会话",
    "type": "list",
    "default": [],
    "hint": "推送的目标会话列表，每行一个会话标识，如 qq:gm:427674145（群聊）或 qq:dm:123456（私聊）"
  }
}
